interface propTypes {
    player: string,
    level: number,
    box: string,
    traps: object,
}

function TrapCard(props: propTypes) {
    return (
        <div>
            test
        </div>
    )
}

export default TrapCard